// property creation of varibale in the class hierarchy. variable inside the class hierarchy
// method is function creation of class hierarchy. function creation of class hierarchy





//






/*class UserEmail { 
        
        userName
        passwors
        userEmail
        age

        by=getelementsByTagName('div').addEventListener("click", function(event){
            event.forEach(element => {
                        
                    });
            }
          );
        validationName(userName){
            const pattern= new RegExp(" a-zA-z0-9 ")
            return pattern.test(userName)
        }

        validationPassword(password){
          const pattern= new RegExp("^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[!@#$%^&*_=+-]).{8,24}$" )
            return pattern.test(password)
        }

        validationEmail(userEmail){

          const pattern=new RegExp("@",".com"," ")
            if(userEmail.contains(pattern))
            return userEmail
        }


        validationAge(age){
            
            
                // const dobInput = document.getElementById("age").value;
                // // Calculate the age of the person based on the input date of birth
                
                const dob = new Date(dobInput);

                age = Math.floor(
                  (Date.now() - dob.getTime()) / (1000 * 60 * 60 * 24 * 365.25)
                );

              
                // Check if the age is within the allowed range
                if (age < 16 || age > 90) {
                  alert("You must be between 16 and 90 years old to use this service.");
                  dobInput.value = "";
                }

                return age   
              }
              
            
        }
        
        */


        

